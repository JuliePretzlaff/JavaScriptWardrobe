@model IEnumerable<WardrobeApp.Models.Shoe>

@{
    ViewBag.Title = "Index";
}
<script src="~/Content/JavaScript.js"></script>


<div class="row title">
    <h2 class="title">Shoes</h2>
</div>
@*filter search button*@
<div class="row">
    <div class="col-md-5 col-sm-0"></div>
    <button id="searchbtn" onclick="searchItem()">Search for shoes by season</button>
</div>
@*//drop down method to call search item function:*@
<div class="row">
    <div class="col-md-5 col-sm-0"></div>
    <select id="mySelect" onchange="searchItem()">
        <option value="Select">Choose a season!
        <option value="Winter">Winter
        <option value="Spring">Spring
        <option value="Summer">Summer
        <option value="Fall">Fall
    </select>
</div>
<p class="center-link">
    @*@Html.ActionLink("Create New", "Create")*@
    <a class="btn btn-default item-index" href="/Shoes/Create">Create New</a>
</p>

<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-md-3 col-sm-6 col-lg-1-xs-12">
            <div class="thumbnail">
                <h3 class="itemName" onmouseover="sizeBigger(this)" onmouseout="sizeNormal(this)">
                    @Html.DisplayFor(modelItem => item.ShoeName)
                </h3>

                <p class="home">
                    <img class="itemPhoto" src="@Html.DisplayFor(modelItem => item.ShoePhoto)" /><br />

                <p class="season">
                    @Html.DisplayFor(modelItem => item.Season.Season1)
                </p>
                <p class="home">
                    @Html.ActionLink("Details", "Details", new { id = item.ShoeID }, new { @class = "nonnavLink" })
                </p>
            </div>
        </div>
    }
</div>
